---
"@blurengine/bebe": minor
---

Add BAUD audio cue authoring, compact audio pack tooling, the root `Audio` runtime service, and an internal dev command for auditioning loaded or inline BAUD cues with an action bar visualizer that keeps richer inline authoring metadata out of shipped audio packs.
